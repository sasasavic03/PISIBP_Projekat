server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/follow_db
    username: postgres
    password: postgres

    jpa:
      hibernate:
        ddl-auto: none
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect