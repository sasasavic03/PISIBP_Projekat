server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/post_db
    username: rosu
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect